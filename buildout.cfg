### BUILDOUT CONFIG FILE ###

[buildout]
# DIRECTORIES
bin-directory = _buildout
parts-directory = _buildout/parts
develop-eggs-directory = _buildout/dev-eggs

# PARTS
parts =
    python

# EGGS
eggs =
	django

# specify a version for ANY egg used :
#  - to ensure repeatability
#  - to make the buildout script run faster
#    (does not need to check for newer versions)
# check the output for picked versions

show-picked-versions = true

# the python interpreter for the buildout environment
[python]
recipe = zc.recipe.egg:script
eggs = ${buildout:eggs}
scripts = python
interpreter = python
extra-paths = ${buildout:directory}

[versions]
python = 2.7
setuptools = 2.2
zc.buildout = 2.2.1

# RECIPES
zc.recipe.egg = 2.0.1

# DEPENDENCIES
django = 1.6.2
